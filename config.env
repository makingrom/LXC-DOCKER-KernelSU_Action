CONFIG_ENV=config.env


# 编译内核基本配置
# 内核源码地址
KERNEL_SOURCE=https://github.com/makingrom/android_kernel_xiaomi_cezanne
# 内核源码分支
KERNEL_SOURCE_BRANCH=r
# 内核编译配置地址
KERNEL_CONFIG=cezanne_user_defconfig
# 安卓版本号（10/11/12/13/14）
ANDROID_VERSION="11"
# 内核版本(4.4/4.14/4.19/5.4)
KERNEL_VERSION="4.14"
# 编译设备的硬件架构 ARM64(x86、x86_64、arm、arm64)
ARCH=arm64
# 骁龙(QUALCOMM)/联发科(MEDIATEK)平台切换 二选一 启用专属内核配置
PLATFORM_QUALCOMM=false
PLATFORM_MEDIATEK=true
# 低端机填Image.gz-dtb，高端机可以选择Image.gz，Image
KERNEL_IMAGE_NAME=Image.gz-dtb
# 是否 将文件名添加到地址目录
ADD_LOCALVERSION_TO_FILENAME=false


# 配置编译源、编译工具
# 关闭「使用自定义 Clang」，改用下方配置的 AOSP 官方 Clang
USE_CUSTOM_CLANG=true
# 自定义 Clang 的仓库地址 / 分支（因 USE_CUSTOM_CLANG=false，这两个参数无效）
CUSTOM_CLANG_SOURCE=https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/master-kernel-build-2022/clang-r450784e.tar.gz
CUSTOM_CLANG_BRANCH=
# 强制指定 Clang 的「架构前缀」为 ARM64 Linux 标准（而非 Android 专属）
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-
# AOSP 官方 Clang 的分支（2022 年内核编译专用分支）
CLANG_BRANCH=master-kernel-build-2022
# AOSP 官方 Clang 的版本号（r450784e 是稳定版）
CLANG_VERSION=r450784e

# 启用 ARM64/ARM32 版本的 GCC 编译器
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
# 关闭「自定义 ARM64 GCC」，用默认版本
USE_CUSTOM_GCC_64=true
CUSTOM_GCC_64_SOURCE=https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/+archive/refs/tags/android-12.1.0_r27.tar.gz
CUSTOM_GCC_64_BRANCH=
# ARM64 GCC 的命令前缀（比如 aarch64-linux-android-gcc）
CUSTOM_GCC_64_BIN=aarch64-linux-android-
# 同理，控制 32 位 ARM GCC，因你是 ARM64 内核，这部分仅作备用
USE_CUSTOM_GCC_32=true
CUSTOM_GCC_32_SOURCE=https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9/+archive/refs/tags/android-12.1.0_r27.tar.gz
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-


# Kernel集成、生成AnyKernel3包
# 关闭 KernelSU 集成
ENABLE_KERNELSU=true
KERNELSU_TAG=main
# 指定要集成的 KernelSU 版本（v0.9.5 是稳定版）
KERNELSU_TAG=v0.9.5
# KernelSU 产物的大小 / 哈希校验值（可选）
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=
# 是否 使用自定义 AnyKernel3（内核打包工具）
USE_CUSTOM_ANYKERNEL3=true
CUSTOM_ANYKERNEL3_SOURCE=https://github.com/osm0sis/AnyKernel3
CUSTOM_ANYKERNEL3_BRANCH=master


# 编译器功能、模块配置
# 是否 需要切换python2，一般不需要，false即可
SWITCH_PYTHON=true
# 是否 关闭 LTO（链接时优化）
DISABLE_LTO=false
# 是否 关闭「警告转错误」（-Werror）
DISABLE_CC_WERROR=true
# 是否 添加 Kprobes 调试配置（KernelSU 部分功能依赖 Kprobes）
ADD_KPROBES_CONFIG=true
# 是否 添加 OverlayFS 配置（文件系统叠加，KernelSU 挂载模块需要）
ADD_OVERLAYFS_CONFIG=true
# 是否 应用 KernelSU 补丁
APPLY_KSU_PATCH=true
# 是否 开启kvm(高端机可用)，百分之90的用户用不到kvm，false即可
ENABLE_KVM=false
# 内核编译链接器，默认可能用 GCC 的 ld，这里强制改用 LLVM 的 ld.lld
EXTRA_CMDS:LD=ld.lld
# 是否 开启LLVM=1 LLVM_IAS=1参数配置
LLVM_CONFIG=true
# 自定义LLVM工具链参数配置
LLVM_PARAMS=
# LLVM_PARAMS="LLVM=1 LLVM_IAS=1 AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip"
# 是否 使用自定义编译版本配置
USE_VERSION_PARAMS_CONFIG=true
# 自定义编译版本配置
VERSION_PARAMS=
# 是否 启用栈保护兼容修复
DISABLE_STACK_PROTECTOR=true
# 编译前 是否 删除系统无用的内核包
REMOVE_UNUSED_PACKAGES=false
# 是否 开启Ccache 缓存
ENABLE_CCACHE=true



# 编译生成文件配置
# 是否 需要dtbo，一般不需要，false即可
NEED_DTBO=false
# 不编译完整的 boot.img（仅生成内核镜像 Image.gz-dtb）
BUILD_BOOT_IMG=false
# 原始 boot.img 的下载地址（用于拼接内核镜像生成新 boot.img）
SOURCE_BOOT_IMAGE=https://raw.githubusercontent.com/xiaoleGun/KernelSU_action/main/boot/boot.img
